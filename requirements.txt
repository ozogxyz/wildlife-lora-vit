# torch/torchvision come from the GCP Deep Learning image (CUDA-matched) — don't pin here.
# fovea (private) is pip-installed over ssh by bootstrap.sh — box ssh key must be on GitHub.
pytorch_pretrained_vit
huggingface_hub
opencv-python-headless
pandas
numpy
scikit-learn
tqdm
