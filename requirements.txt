# torch/torchvision come from the GCP Deep Learning image (CUDA-matched) — don't pin here.
# fovea is private; bootstrap.sh installs it via GH_PAT.
pytorch_pretrained_vit
huggingface_hub
opencv-python-headless
pandas
numpy
scikit-learn
tqdm
